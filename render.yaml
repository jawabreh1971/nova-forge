services:
  - type: web
    name: atlas-nova
    env: python
    plan: starter
    region: frankfurt
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn nova.app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /healthz
