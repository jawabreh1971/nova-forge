services:
  - type: web
    name: nova-forge
    env: python
    plan: free
    buildCommand: |
      pip install -r backend/requirements.txt
    startCommand: |
      cd backend && bash start.sh
    autoDeploy: true
